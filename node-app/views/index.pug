extends default
block content
  div.container
    each person in people
      div.person
        div.person-image(style=`background: url('/images/Cat03.jpg') top center
        no-repeat; background-size: cover;`)
        h2.person-name
          | #{person.name}
        h2.person-name
          | #{person.age}